# Kubernetes Service Account with Workload Identity
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${APP_NAME}-sa
  labels:
    app: ${APP_NAME}
  annotations:
    # Client ID will be patched by overlay or Flux substitution
    azure.workload.identity/client-id: ${IDENTITY_CLIENT_ID}
